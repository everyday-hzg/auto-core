package ${bussPackage}.${entityPackage}.dao.impl;
import javax.annotation.Resource;

import org.apache.log4j.Logger;

/**
 * Title: ${className}DaoImplTest
 * Description: ${codeName}DAO测试类
 * @author: ${author} ${yyyyMMddHHmmss}
 */
 
public class ${className}DaoImplTest {

	/**初始化日志对象*/
	private final static Logger logger = Logger.getLogger(${className}DaoImplTest.class);
	
}
